// import classes from '*.module.css';
import React, { SVGProps } from 'react';

// Logo FDP
const Logo = (props: SVGProps<SVGSVGElement>) => (
    <div style={{fontSize:'22px', fontWeight:'normal'}}>FDP Connect ™</div>
);

export default Logo;
